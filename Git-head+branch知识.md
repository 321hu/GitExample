# Git基本知识

### Head

- **Head**是一个指向当前分支或提交的指针。在大多数情况下，Head指向的是一个分支名，即你当前所在的分支。当你进行提交时，新提交会在Head所指向的分支上。
- 如果你使用`git checkout`切换到一个特定的提交记录，Head将指向那个提交记录，这种状态被称为“分离的Head状态”。

### 分支（Branch）

- **分支**是一个指向提交记录的可移动指针。默认分支通常是`master`或`main`。
- 分支实际上是一个指向提交对象的引用，记录了从一个提交到另一个提交的历史。
- 当你在某个分支上进行新的提交时，该分支会移动到新的提交上。

### 提交记录（Commit）

- **提交记录**是Git中的基本单元，每个提交都有一个唯一的SHA-1哈希值作为标识。
- 提交记录包含了对项目的改动、提交信息、作者信息以及指向父提交（或多个父提交，若是合并提交）的引用。
- 提交记录形成了一条从当前提交回溯到初始提交的链条。

### 三者之间的关系

- **Head和分支的关系**：Head通常指向一个分支。当Head指向一个分支时，任何新的提交都会更新该分支，使其指向最新的提交。
- **分支和提交记录的关系**：分支是对提交记录的引用。分支会指向最新的提交。历史提交形成了一条链，从当前提交可以追溯到初始提交。
- **Head和提交记录的关系**：当Head处于分离状态时，它直接指向一个特定的提交，而不是一个分支。在这种状态下，新的提交不会更新任何分支。

### 示例

假设你的项目有如下提交记录：

```
A - B - C (main)
```

- `main`分支指向提交`C`。
- Head指向`main`，表示你当前在`main`分支上。

当你在`main`分支上进行一次新的提交`D`时：

```
A - B - C - D (main)
                ^
               Head
```

- `main`分支和Head都移动到了新的提交`D`。

如果你切换到`feature`分支：

```
A - B - C - D (main)
     \
      E - F (feature)
```

- `feature`分支指向提交`F`。
- `main`分支仍指向提交`D`。
- Head现在指向`feature`。

当你在`feature`分支上进行一次新的提交`G`时：

```
A - B - C - D (main)
     \
      E - F - G (feature)
                  ^
                 Head
```

- `feature`分支和Head都移动到了新的提交`G`。

这样，分支和提交记录的关系形成了一条历史链条，而Head指示了你当前的工作位置。

当你执行`git checkout Head^`操作时：

```
A - B - C - D (main)
     \
      E - F - G (feature)
          ^
         Head
```

- 此时Head指向提交F